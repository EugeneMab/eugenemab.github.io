# Implementation Steps

## Step 1: Infrastructure & UI Update
- [x] Update `tsconfig.json` to output to `pub/js`.
- [x] Update `index.html` to load `js/main.js`.
- [x] Verify `build.cmd` works with the new structure.

## Step 2: Lexer (Python Subset)
- [ ] Implement `src/lexer.ts`.
- [ ] Handle keywords, identifiers, literals, and operators.
- [ ] **Crucial:** Implement indentation tracking to emit `INDENT` and `DEDENT` tokens.
- [ ] **Testing:** Create `src/lexer.test.ts` and verify with `basic.py` and `variables.py`.
- [ ] **UI Integration:** Display tokens in the "Lexicon" tab.

## Step 3: Parser (AST)
- [ ] Implement `src/parser.ts`.
- [ ] Build a Recursive Descent Parser.
- [ ] Support Function definitions (`def`), Assignments, and Arithmetic.
- [ ] **Testing:** Create `src/parser.test.ts` and verify AST structure for samples.
- [ ] **UI Integration:** Display AST JSON in the "AST" tab.

## Step 4: Emitter (WAT & WASM)
- [ ] Implement `src/compiler.ts`.
- [ ] Generate human-readable `.wat` format (LISP-like).
- [ ] Encode binary `.wasm` format (LEB128 encoding, Section headers).
- [ ] **Testing:** Create `src/compiler.test.ts` and compare WAT/WASM output with manual expectations.
- [ ] **UI Integration:** Display WAT and Hex in respective tabs.

## Step 5: Execution & Integration
- [ ] Connect `main.ts` to the pipeline.
- [ ] Use `WebAssembly.instantiate` to run generated bytes.
- [ ] Capture the return value of `main()` and display in UI.
- [ ] **Testing:** End-to-end test with all samples.
- [ ] **Final Check:** Run `check.cmd` to verify linting and all tests.
