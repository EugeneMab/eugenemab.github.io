// Simplified modules sample for current compiler subset
// (parser does not support inline `mod {}` blocks yet)

fn add_to_waitlist() {
    println!(42);
}

fn main() {
    add_to_waitlist();
    0
}
